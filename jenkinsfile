def abc = sh "uname"

pipeline {
    agent any
    environment {
          MY_FILES = "sh(script: 'cd mydir && ls -l', returnStdout: true)"
        }
    stages {
        stage ('test') {
            steps {
                echo "${MY_FILES}"
                echo "${abc}"
                }
            }
        }
    }