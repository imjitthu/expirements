def var = sh(script: 'uname', returnStdout: true)
println var

pipeline {
    agent any
    stages {
        stage ('Test') {
            steps {
                echo "hello world"
            }
        }
    }
}