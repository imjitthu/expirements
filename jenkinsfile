def ret = sh(script: 'uname', returnStdout: true)
println ret

pipeline {
    agent any
    stages {
        stage ('Test') {
            steps {
                echo "hello world"
            }
        }
    }
}