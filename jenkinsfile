def var = sh(script: 'uname', returnStdout: true)

pipeline {
    agent any
    stages {
        stage ('Test') {
            steps {
                echo "${var}"
                println var
            }
        }
    }
}