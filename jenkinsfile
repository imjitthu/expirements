def image_tag = "ABC"


//  = sh(script: 'uname', returnStdout: true)

pipeline {
    agent any
    stages {
        stage ('Test') {
            steps {
                // script {
                // image_tag = "ABC"
                //     }
                echo "${image_tag}"
            }
        }
    }
}