def abc = "ABC"
pipeline {
    agent any
    environment {
          MY_FILES = "sh(script: 'cd mydir && ls -l', returnStdout: true)"
        }
    stages {
        stage ('Test') {
            steps {
                echo "${MY_FILES}"
                echo "${abc}"
                }
            }
        }
    }