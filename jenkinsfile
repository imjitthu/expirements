def GIT_TAG = sh(script: 'uname -m', returnStdout: true)

pipeline {
    agent any
    stages {
        stage ('Test') {
            steps {
                echo "${GIT_TAG}"
            }
        }
    }
}